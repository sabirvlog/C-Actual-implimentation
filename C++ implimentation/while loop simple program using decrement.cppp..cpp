#include<iostream>
#include<conio.h>
using namespace std;
int main()
  
  {
  
  	int x=100; // initionalization of a variable
  	while(x>=1) // here we use (while loop) .we will execute this loop till hundred. 
   {
   	cout<< x <<endl; // output statement used as an extraction operator to print the variable on a screen.
   	x--; // we used it as a decrement operator(decrement is the process of subtraction in this we will print 
              // numbers from greater to lower in dessending order ) with the help of this statement we
   }           //  print 100 numbers on the screeen in one statement to simplify the programe.
   return 0;
   getch();
}
