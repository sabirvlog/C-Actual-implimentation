#include<iostream>
using namespace std;
int main()
{
	
	int n,i; 
	
	for( i=n; i<=n;++i) // in this statement we have number n which is equal to index i (i=n) its necessary because as "i" 
	// will be increases "n" also increases as same sa "i" because when we enter any number lets suppose (i=3=n) its means
	// i=3 & n=3 also and after that we use the increment signs before (i) "++i" it means that the value of "n" will be increases as same as value of "i". 
	
	cout<"Enter the cube. ";
	cin>>i;
	cout<<"cube is\n "<<i<<" "<<i*i*i;
}

